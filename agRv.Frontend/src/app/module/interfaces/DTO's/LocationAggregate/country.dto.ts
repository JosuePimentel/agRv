import { BaseLocationDto } from '../Base/baseLocation.dto';

export interface CountryDto extends BaseLocationDto {
  // state: StateDto[]; // Comentado porque não é necessário aqui
  // person: PersonDto[]; // Comentado porque não é necessário aqui
}
